const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target);}});},{threshold:0,rootMargin:'0px 0px 0px 0px'});reveals.forEach(r=>observer.observe(r));setTimeout(()=>{reveals.forEach(r=>r.classList.add('visible'));},1500);async function handleSubmit(e){e.preventDefault();const form=e.target;const btn=form.querySelector('.form-submit');const successMsg=document.getElementById('successMsg');const errorMsg=document.getElementById('errorMsg');btn.textContent='Gönderiliyor...';btn.disabled=true;successMsg.style.display='none';errorMsg.style.display='none';try{const formData=new FormData(form);const response=await fetch(form.action,{method:'POST',body:formData,headers:{'Accept':'application/json'}});if(response.ok){successMsg.style.display='block';form.reset();}else{const data=await response.json();if(Object.hasOwn(data,'errors')){errorMsg.textContent=data["errors"].map(error=>error["message"]).join(",");}else{errorMsg.textContent="Bir hata oluştu,lütfen tekrar deneyin.";}errorMsg.style.display='block';}}catch(error){errorMsg.textContent="Bağlantı hatası,lütfen internetinizi kontrol edin.";errorMsg.style.display='block';}finally{btn.textContent='Gönder';btn.disabled=false;setTimeout(()=>{successMsg.style.display='none';errorMsg.style.display='none';},5000);}}document.addEventListener('DOMContentLoaded',function(){var banner=document.getElementById('cookieBanner');if(!banner)return;if(localStorage.getItem('cookie_consent'))return;setTimeout(function(){banner.style.display='flex'},100);function dismissBanner(){banner.classList.add('cookie-hidden');setTimeout(function(){banner.style.display='none'},500)}document.getElementById('cookieAccept').addEventListener('click',function(){localStorage.setItem('cookie_consent','accepted');dismissBanner()});document.getElementById('cookieDecline').addEventListener('click',function(){localStorage.setItem('cookie_consent','declined');dismissBanner()})});
